# ============================================
# 广告案例库后端服务环境变量配置
# ============================================
# 复制此文件为 .env 并修改相应的配置值
# cp env.example .env

# ============================================
# API 配置
# ============================================
API_HOST=0.0.0.0
API_PORT=8000

# 热重载配置（开发环境推荐启用）
# true: 启用热重载，修改代码后自动重启（开发环境）
# false: 禁用热重载，使用多 worker（生产环境）
API_RELOAD=true

# Worker 数量（仅在 API_RELOAD=false 时生效）
API_WORKERS=1

# ============================================
# 数据库配置
# ============================================
DB_HOST=localhost
DB_PORT=5432

# ⚠️ 重要：数据库名称固定为 ad_case_db，不要修改
DB_NAME=ad_case_db

# 数据库用户
DB_USER=bing

# 数据库密码（如果未设置密码则留空）
DB_PASSWORD=

# 数据库连接池配置
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# ============================================
# 向量模型配置
# ============================================
# ⚠️ 重要：必须配置本地模型路径，避免在线下载失败
# 模型名称: BAAI/bge-large-zh-v1.5
VECTOR_MODEL_PATH=/Users/bing/.cache/huggingface/hub/models--BAAI--bge-large-zh-v1.5

# 向量维度（固定值，不要修改）
VECTOR_DIMENSION=1024

# ⚠️ 重要：离线模式，必须设置为 true，使用本地模型
# true: 使用本地模型，避免请求 HuggingFace（推荐）
# false: 允许在线下载模型（不推荐，可能失败）
VECTOR_OFFLINE_MODE=true

# ============================================
# 缓存配置（可选）
# ============================================
# Redis 配置（如果不需要缓存功能，可以保持注释状态）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_ENABLED=false

# ============================================
# 日志配置
# ============================================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# 代理配置（可选）
# ============================================
# 如果需要使用代理访问外部服务（如 HuggingFace），可以配置以下选项
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# ============================================
# Clash Verge API 配置（用于自动切换代理节点，避免被封禁）
# ============================================
# 如果使用 Clash Verge 代理，可以配置以下选项实现定时自动切换节点
# CLASH_API_URL=http://127.0.0.1:9097
# CLASH_SECRET=123456
# CLASH_PROXY_GROUP=GLOBAL
# CLASH_SWITCH_MODE=hybrid  # 切换模式: count(按请求次数)/time(按时间)/hybrid(混合)
# CLASH_SWITCH_INTERVAL=50  # 每 N 个请求切换一次（count 或 hybrid 模式）
# CLASH_SWITCH_INTERVAL_MINUTES=10  # 每 N 分钟切换一次（time 或 hybrid 模式）
# CLASH_AUTO_SWITCH_ON_ERROR=true  # 请求失败时自动切换节点
